# 10 days plan -> 15-08-2026

- Exam on 26th August

- Use HackerRank for story based questions

---



# Finally: typing practice

This is **VERY important**.

I don't want you merely looking at code and saying:

> "Yeah yeah, I understand this."

That's not enough.

You'll practice typing things like:

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;

    for(int i = 0; i < n; i++) {
        cout << i << " ";
    }

    return 0;
}
```

**without looking at it.**

---

## Your biggest goal

When you see a question like:

> Find the frequency of each element in an array.

I want your brain to immediately go:

**"map → input → loop → `mp[x]++` → print."**

Not:

> "Wait... how do I declare a map in C++ again?"

😂

That's the difference we're trying to create.
